jQuery(document).ready(function($){
	var easing = 'swing';
	$(#slider).parallaxSlider({
		autoPlay5000,false to disable, otherwise int duration in ms hold between slides
		speed2000, transition duration between slides
		easing 'swing',
		numBackgrounds 4,
		thumbRotationfalse,
		 hash false,
		 debugtrue,
		css3 true,
		easingCSS3 'cubic-bezier(0.000, 0.265, 1.000, 0.005)',
	});
})